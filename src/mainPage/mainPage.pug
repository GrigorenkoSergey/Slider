extends ../assets/blocks/template/template.pug

block includes

block variables
    - 
        var titleName = 'SLIDERS'
        var optionsInputs = ["min", "max", "step", "angle",
            "thumbLeftPos", "thumbRightPos"];
        var optionsRadio = ["range", "hintAboveThumb"];

block content

    .slider-container1
        .slider1
        -
            let i = 65;
            let arr = new Array(58).fill(1).map(item => i++);
            let pContent = String.fromCodePoint(...arr);

        p=  pContent 


    .slider-container2
        .slider2
        span A
        .slider5

    .slider-container3
        .imgSprite
        .slider3
    .examples
        .slider-container4
            .slider4
        ul.slider-options
            each val in optionsInputs
                li.slider-options__li= val
                    input.slider-options__input(name = val value = val data-id= "slider4")
