export type ViewOptions = {
  className?: string,
  selector?: string,
  angle?: number,
  step?: number,
  range?: boolean,
  hintAboveThumb?: boolean,
  hintAlwaysShow?: boolean,
  showScale?: boolean,
  partsAmount?: number,
}
