export type ModelOptions = {
  min?: number,
  max?: number,
  step?: number,
  partsAmount?: number,
  thumbLeftValue?: number,
  thumbRightValue?: number | null,
  range?: boolean,
  precision?: number,
  alternativeRange?: string[],
}
